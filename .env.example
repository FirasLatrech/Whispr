# Socket.IO Server URL
# For production, set this to your production URL (e.g., https://whispr-sage.vercel.app)
# For local development, leave empty to use window.location.origin
NEXT_PUBLIC_SOCKET_URL=

# Allowed CORS Origins
# Comma-separated list of allowed origins for Socket.IO connections
# For production, set this to your production domain(s)
# For local development, leave empty to allow all origins (*)
ALLOWED_ORIGINS=

# Server Configuration (optional)
# HOSTNAME=localhost
# PORT=3000
